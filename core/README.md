# DjangoKit Core

> DjangoKit is Django + filesystem routing + TOML settings + scaffolding.
> See https://djangokit.wyattbaldwin.com/ for more information.

This package implements the core functionality of DjangoKit.

## Features

- Easily set up a Django project with filesystem routing
- Easily create APIs
- Convention over configuration
- Env-specific configuration using TOML files (or using a standard
  Django settings module)
- Builtin support for configuration using environment variables
- Tooling to create models, routes, etc
- Full power of Django available
- Supports Python 3.10+
- Supports Node 22+
- Supports Django 4, 5, and 6 (latest point release of each)

## Getting Started

- Docs: https://djangokit.wyattbaldwin.com/docs/get-started
