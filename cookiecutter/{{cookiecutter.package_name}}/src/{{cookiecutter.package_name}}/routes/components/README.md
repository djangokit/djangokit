This is a good place for reusable Django components (template partials).

> ALL Django templates for your app, including components, must be
> located somewhere under the `routes` directory for Django to find
> them.
